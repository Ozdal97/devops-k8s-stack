replicaCount: 1
image:
  tag: "staging"
ingress:
  hosts:
    - host: api.staging.example.com
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
